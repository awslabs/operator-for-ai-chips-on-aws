---
description: "Write tests and production code via TDD (changes left uncommitted)"
---
# implement · code

Read `.kiro/skills/implement/skills/controller.md` and follow it; run the
**code** phase against the current issue's plan
(`.artifacts/implement/<issue-id>/02-plan.md`).

If exactly one issue-id exists, use it; if several exist, **ask me which one**.
All code changes are left uncommitted in the working tree for review.
