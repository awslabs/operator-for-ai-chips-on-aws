---
description: "Run unit tests and iterate on failures"
---
# implement · validate

Read `.kiro/skills/implement/skills/controller.md` and follow it; run the
**validate** phase for the current issue
(`.artifacts/implement/<issue-id>/`).

If exactly one issue-id exists, use it; if several exist, **ask me which one**.
